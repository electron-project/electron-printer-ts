import { createMainWindow } from '@/main/window/main'

const ElectronWins = {
  main: createMainWindow(),
}

const ElectronWinCreateFn = {
  main: createMainWindow(),
}

const createWindow = () => {}
